# .github/workflows/azure-static-web-apps.yml
name: Azure Static Web Apps - Jekyll Site Deployment

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. リポジトリのチェックアウト
      - name: 🚀 Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: true

      # 2. Ruby 環境セットアップ
      - name: 💎 Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true

      # 3. Gemfile存在確認
      - name: 📦 Ensure Gemfile exists
        run: test -f Gemfile || (echo "Gemfile not found. Cannot install dependencies." && exit 1)

      # 4. 依存関係インストールと Jekyll ビルド
      - name: 📦 Install Dependencies and Build Jekyll Site
        run: |
          bundle install
          bundle exec jekyll build

      # 5. ビルド成果物チェック
      - name: 🏗 Check Build Output
        run: test -d _site || (echo "No _site directory found. Build failed." && exit 1)

      # 6. SWA へのデプロイ（ビルドはスキップ、成果物アップロードのみ）
      - name: 🌐 Deploy to Azure Static Web Apps
        uses: azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          app_location: "/"           # ルート明示
          output_location: "_site"    # ビルド成果物
          api_location: ""
          skip_api_build: true
          skip_app_build: true        # GitHub Actionsで既にビルド済み
          action: "upload"            # 明示的にアップロードモード指定
