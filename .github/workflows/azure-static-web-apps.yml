---

## II. 最終最適化版 GitHub Actions ワークフロー

ファイル名: `.github/workflows/azure-static-web-apps.yml`

```yaml
# .github/workflows/azure-static-web-apps.yml

name: Azure Static Web Apps - Jekyll Site Deployment

on:
  push:
    branches:
      - main
  workflow_dispatch:

# 修正1: 権限を明示
permissions:
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      # 1. リポジトリのチェックアウト
      - name: 🚀 Checkout Repository
        uses: actions/checkout@v4
        with:
          # 修正2: 不要なサブモジュール取得をスキップ
          submodules: false 

      # 2. Ruby環境セットアップ
      - name: 💎 Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          # 修正3: 安定性を重視し、実績のあるバージョンに固定
          ruby-version: '3.2'
          bundler-cache: true

      # 3. 依存関係インストールとビルド
      - name: 📦 Install Dependencies and Build Jekyll Site
        run: |
          bundle install
          
          # 修正4: デバッグ容易性のために --trace を追加
          bundle exec jekyll build --trace

      # 4. SWAへのデプロイ
      - name: 🌐 Deploy to Azure Static Web Apps
        # 修正5/エラー対応: v2が見つからないため、安定版の v1 に戻す
        uses: azure/static-web-apps-deploy@v1 
        with:
          # シークレットを使用
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          
          # 修正1: SWA設定 - リポジトリルートをソースコードの場所とする
          app_location: "." 
          # 修正6: SWA設定 - Jekyllのビルド成果物ディレクトリ
          output_location: "_site" 
          api_location: ""
          skip_api_build: true
